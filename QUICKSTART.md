# Быстрый старт

Пошаговая инструкция для запуска системы за 1-2 часа.

---

## ✅ Что нужно иметь

- [ ] Anthropic API ключ (получить на [console.anthropic.com](https://console.anthropic.com))
- [ ] Telegram аккаунт
- [ ] VPS сервер (или можно запустить локально для теста)

---

## 🚀 Запуск за 6 шагов

### 1. Создать Telegram канал (5 минут)

```
1. Telegram → ☰ → Новый канал
2. Название: "Умная аналитика для маркетплейсов"
3. Username: @smart_analytics_mp (или любой свободный)
4. Публичный канал → Создать
```

### 2. Создать Telegram бота (3 минуты)

```
1. Найти @BotFather
2. /newbot
3. Имя: Smart Analytics Agent
4. Username: @smart_analytics_agent_bot
5. Сохранить Bot Token
```

### 3. Добавить бота в канал (2 минуты)

```
1. Открыть канал → Администраторы → Добавить
2. Найти @smart_analytics_agent_bot
3. Права: ✅ Публикация, ✅ Редактирование, ✅ Удаление
```

### 4. Клонировать проект (1 минута)

```bash
git clone https://github.com/<username>/telegram-ai-content-agent.git
cd telegram-ai-content-agent
```

### 5. Настроить окружение (5 минут)

```bash
# Создать виртуальное окружение
python3.11 -m venv venv
source venv/bin/activate  # Linux/Mac

# Установить зависимости
pip install -r requirements.txt

# Создать .env
cp .env.example .env
nano .env
```

**Заполнить .env:**
```env
ANTHROPIC_API_KEY=sk-ant-api03-...  # Ваш ключ от Anthropic
TELEGRAM_BOT_TOKEN=123456:ABC...     # Token от @BotFather
TELEGRAM_CHANNEL_ID=@smart_analytics_mp  # Username канала
TELEGRAM_ADMIN_ID=123456789          # Ваш Telegram ID (@userinfobot)
```

### 6. Запустить локально (2 минуты)

```bash
# Запустить PostgreSQL
docker compose up -d postgres

# Применить миграции
alembic upgrade head

# Запустить генерацию поста (БЕЗ публикации)
python -m app.main
```

**Результат:** Должен сгенерироваться пост и вывестись в консоль.

---

## 🎯 Первая публикация

После проверки что всё работает:

1. Откройте `app/main.py`
2. Найдите строку: `await pipeline.run_once(publish=False)`
3. Измените на: `await pipeline.run_once(publish=True)`
4. Запустите снова: `python -m app.main`

**Проверьте канал** - должен появиться первый пост! 🎉

---

## 🔄 Автоматизация (опционально)

Для автоматической работы 24/7:

1. Арендуйте VPS (от 500₽/мес)
2. Следуйте инструкции в **[DEPLOYMENT.md](DEPLOYMENT.md)**
3. Настройте scheduler (Этап 3 в **[PLAN.md](PLAN.md)**)

---

## ❓ Проблемы?

### Ошибка подключения к БД

```bash
# Убедитесь что PostgreSQL запущена
docker compose ps

# Перезапустить если нужно
docker compose restart postgres
```

### Claude API не отвечает

- Проверьте баланс на console.anthropic.com
- Проверьте что ANTHROPIC_API_KEY правильный в .env

### Бот не может постить в канал

- Проверьте что бот добавлен как администратор
- Проверьте что TELEGRAM_CHANNEL_ID правильный (с @)
- Попробуйте использовать числовой ID вместо username

---

## 📚 Дополнительно

- **Полная документация:** [DEPLOYMENT.md](DEPLOYMENT.md)
- **План разработки:** [PLAN.md](PLAN.md)
- **Прогресс:** [PROGRESS.md](PROGRESS.md)

---

**Готово!** Система работает локально. Теперь можно настроить автоматизацию или деплой на сервер.
